import Home from './Home';

const HomeConfig = {
  settings: {
    layout: {
      config: {},
    },
  },
  routes: [
    {
      path: '/',
      component: Home,
    },
  ],
};

export default HomeConfig;
