import Dashboard from './DashboardForm';

const DashboardConfig = {
  settings: {
    layout: {
      config: {},
    },
  },
  routes: [
    {
      path: '/',
      component: Dashboard,
    },
  ],
};

export default DashboardConfig;
